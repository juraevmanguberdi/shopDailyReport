<template>
  <div class="row justify-content-center">
    <div class="col-8">
      <div v-if="paymentMethod">
        <h2 class="jh-entity-heading" data-cy="paymentMethodDetailsHeading">
          <span v-text="$t('shopDailyReportApp.paymentMethod.detail.title')">PaymentMethod</span> {{ paymentMethod.id }}
        </h2>
        <dl class="row jh-entity-details">
          <dt>
            <span v-text="$t('shopDailyReportApp.paymentMethod.name')">Name</span>
          </dt>
          <dd>
            <span>{{ paymentMethod.name }}</span>
          </dd>
          <dt>
            <span v-text="$t('shopDailyReportApp.paymentMethod.code')">Code</span>
          </dt>
          <dd>
            <span>{{ paymentMethod.code }}</span>
          </dd>
          <dt>
            <span v-text="$t('shopDailyReportApp.paymentMethod.note')">Note</span>
          </dt>
          <dd>
            <span>{{ paymentMethod.note }}</span>
          </dd>
        </dl>
        <button type="submit" v-on:click.prevent="previousState()" class="btn btn-info" data-cy="entityDetailsBackButton">
          <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.back')"> Back</span>
        </button>
        <router-link
          v-if="paymentMethod.id"
          :to="{ name: 'PaymentMethodEdit', params: { paymentMethodId: paymentMethod.id } }"
          custom
          v-slot="{ navigate }"
        >
          <button @click="navigate" class="btn btn-primary">
            <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.edit')"> Edit</span>
          </button>
        </router-link>
      </div>
    </div>
  </div>
</template>

<script lang="ts" src="./payment-method-details.component.ts"></script>
