import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { ILiabilityRegistry } from '@/shared/model/liability-registry.model';

const baseApiUrl = 'api/liability-registries';

export default class LiabilityRegistryService {
  public find(id: number): Promise<ILiabilityRegistry> {
    return new Promise<ILiabilityRegistry>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: ILiabilityRegistry): Promise<ILiabilityRegistry> {
    return new Promise<ILiabilityRegistry>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: ILiabilityRegistry): Promise<ILiabilityRegistry> {
    return new Promise<ILiabilityRegistry>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: ILiabilityRegistry): Promise<ILiabilityRegistry> {
    return new Promise<ILiabilityRegistry>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
